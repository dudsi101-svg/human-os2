# Rejestr ryzyk — Dzik OS MVP

Status: aktualny na 2026-08-18. Ryzyka zaakceptowane świadomie dla MVP;
każde ma wskazany warunek ponownej oceny.

| ID | Ryzyko | Waga | Mitygacja w MVP | Do zrobienia przed produkcją |
|---|---|---|---|---|
| R-01 | Dane zdrowotne bez formalnej oceny prawnej (RODO art. 9) | WYSOKA | mapa przetwarzania, zgody wersjonowane, eksport/usunięcie wbudowane | przegląd prawny, umowa powierzenia, polityka prywatności |
| R-02 | Brak szyfrowania at-rest bazy i plików | ŚREDNIA (**pliki ZAMKNIĘTE 2026-08-18**) | **Pliki uploadów: szyfrowanie AES-256-GCM przy zapisie/odczycie** (`storage.py`, klucz `DZIK_FILE_KEY` — DEPLOYMENT §4b; pliki sprzed włączenia klucza czytane wprost, brak cichego mieszania trybów; klucz przechowywany osobno od backupów). Otwarte: szyfrowanie samej bazy | baza: dysk szyfrowany / pgcrypto; na Fly wolumeny są szyfrowane at-rest na poziomie bloków |
| R-03 | Rate limiter logowania w pamięci procesu (reset przy restarcie, nie działa między replikami) | ŚREDNIA | pojedynczy proces w MVP | licznik w DB/Redis |
| R-04 | Łańcuch audytu w osobnym pliku SQLite — zdarzenie może zostać zapisane mimo rollbacku transakcji głównej DB (nadmiarowy wpis, nigdy brak wpisu) | ŚREDNIA | akceptowalne: fałszywie dodatnie wpisy audytu nie ukrywają operacji | outbox pattern lub StateCheckpoint z hos_engine |
| R-05 | ~~Zgoda onboardingowa bez potwierdzenia podmiotu~~ **ZAMKNIĘTE 2026-08-17**: klient przy pierwszym logowaniu jawnie potwierdza zgodę (CONSENT_CONFIRMED w audycie) albo ją cofa | — | brama zgód w aplikacji + endpoint confirm | — |
| R-06 | ~~Brak wymuszenia zmiany hasła startowego~~ **ZAMKNIĘTE 2026-08-17**: konto z hasłem startowym jest blokowane po stronie serwera (PASSWORD_CHANGE_REQUIRED) do czasu zmiany; zmiana unieważnia pozostałe sesje | — | wymuszona zmiana przy 1. logowaniu | reset hasła e-mailem (odłożone) |
| R-07 | Brak 2FA | NISKA (skala MVP) | silne hasła, rate limiting, sesje wygasające | TOTP dla trenera i admina |
| R-08 | Filmy MP4 przechowywane bez skanowania/transkodowania | NISKA | whitelist typów, limit rozmiaru, serwowanie z nosniff | antywirus/transkodowanie przy większej skali |
| R-09 | SQLite jako domyślna baza (pojedynczy proces) | NISKA | Compose z PostgreSQL gotowy | produkcja wyłącznie na PostgreSQL |
| R-18 | **Integralność referencyjna nigdy nie była egzekwowana**: SQLite domyślnie nie sprawdza kluczy obcych, więc kod zapisywał wiersze wskazujące na jeszcze nieistniejące rekordy (rola przed kontem, relacja przed klientem, rata przed harmonogramem). Na PostgreSQL każdy taki zapis kończy się błędem — ujawnione 18.08.2026 przy pierwszym uruchomieniu zestawu na PG | **BYŁA WYSOKA** (wdrożenie na PostgreSQL wywracało zakładanie klienta przez trenera) | naprawione w `routers/clients.py`, `seed.py`, `simulate.py` i fixture'ach testowych (`db.flush()` przed zapisem zależnych wierszy); zestaw testów uruchamiany na PostgreSQL w CI (job `backend-postgres`) jako stała bramka | **ZAMKNIĘTE 2026-08-18**: `PRAGMA foreign_keys=ON` włączana na każdym połączeniu SQLite (`db.py`, `event.listens_for(engine, "connect")`) — ta klasa błędów wychodzi teraz lokalnie, nie dopiero na PG |
| R-10 | Napięcie normatywne: INTENDED_PURPOSE.md Human OS (aplikacja osobista bez suplementów) vs. harmonogram suplementów w Dzik OS | ŚREDNIA | Dzik OS wyłącznie **przechowuje i przypomina** plan wpisany przez człowieka z zapisanym autorem; zero rekomendacji i dawkowania przez system — patrz ADR-DZIK-003 §4 | opinia prawna dot. granicy wyrobu medycznego przy rozwoju funkcji |
| R-11 | E-maile klientów widoczne dla admina (dane kontaktowe, nie zdrowotne) | NISKA | audyt każdego wejścia admina | rozważyć maskowanie |
| R-12 | ~~Brak kopii zapasowych w MVP~~ **ZAMKNIĘTE 2026-08-18**: `python -m dzik_os.backup` — spójne archiwum (baza przez sqlite3 backup API / `pg_dump`, audit.db, uploady w postaci zaszyfrowanej), retencja `DZIK_BACKUP_KEEP`, restore z weryfikacją łańcucha audytu i odmową nadpisania bez `--force`; pełny cykl backup→utrata→restore pokryty testem (`test_backup.py`) | — | narzędzie + test odtwarzania w repo; snapshoty wolumenów Fly jako druga warstwa | pozostaje operacyjne: włączenie harmonogramu (cron/GitHub Actions — DEPLOYMENT §4a) i kopia archiwów poza maszynę |
| R-13 | Dziennik obserwacji mógłby zostać odczytany jako narzędzie diagnostyczne | ŚREDNIA | system zapisuje tekst dosłownie, bez interpretacji; NIEPOKOJACE to wyłącznie flaga do przeglądu przez trenera (nigdy autouzupełniana diagnoza/sugestia); komunikat w UI wprost mówi „to nie jest diagnoza" | jeśli funkcja urośnie: przegląd prawny granicy wyrobu medycznego (jak R-10) |
| R-14 | Powiadomienia e-mail o niepokojącej obserwacji są domyślnie wyłączone (`NullNotificationProvider` nic nie wysyła) — trener może nie zauważyć na czas, jeśli poleganie wyłącznie na e-mailu | NISKA | główny kanał to badge/filtr w panelu trenera (zawsze aktywny, niezależny od e-maila); e-mail to tylko dodatkowe wzmocnienie | podłączenie realnego dostawcy e-mail (decyzja operatora, klucze poza repo) |
| R-15 | Treść bazy wiedzy pisze i publikuje trener bez recenzji/moderacji systemowej | NISKA | jawna informacja w UI trenera („treść i jej poprawność są po Twojej stronie"); broadcast tylko do własnych klientów, nie publiczne | jeśli funkcja urośnie do wielu trenerów: rozważyć politykę treści |
| R-16 | Podsumowania AI raportów (`ai_provider.py`) są domyślnie wyłączone (`NullAIProvider`) — funkcja widoczna w UI, ale nieaktywna do czasu konfiguracji | NISKA (świadomie propose-only) | jawny komunikat „wymaga konfiguracji" zamiast cichego błędu; żadna odpowiedź AI nie trafia do klienta bez edycji/zatwierdzenia przez trenera | decyzja operatora o dostawcy + zakresie danych wysyłanych do AI (patrz DATA_PROCESSING_MAP §AI) |
| R-17 | Przepisany ze zdjęcia tekst bywa błędny (etykieta pod kątem, pismo odręczne, kolumna „na porcję” zamiast „w 100 g”) — błędna wartość odżywcza mogłaby wejść do bazy produktów i planu | ŚREDNIA | wynik jest ZAWSZE propozycją: tekst stoi obok zdjęcia, wartości są edytowalne, zapis następuje dopiero po zatwierdzeniu przez człowieka; zakresy walidacji jak przy imporcie CSV; pole nieodczytane zostaje PUSTE (nigdy zgadywane), a UI wypisuje, czego nie udało się odczytać; produkt niesie proweniencję (OCR + plik źródłowy + silnik) | ograniczenia spisane w `OCR.md` §7; przy rozroście funkcji rozważyć wymóg potwierdzenia wartości przez drugi odczyt |
| R-19 | ~~**Import arkusza zużywa pamięć proporcjonalną do PLIKU, nie do limitu wierszy**~~ **ZAMKNIĘTE 2026-08-18**, obie drogi naprawione i zmierzone ponownie. **(a) Bomba dekompresyjna w `.xlsx`:** przed — plik 1,64 MB rozpakowujący się do 423 MB kosztował **1164 MB RSS i 129 s**; po — **53 MB i 0,00 s**, odrzucony z komunikatem. Kluczem okazał się nie odczyt wierszy, tylko `load_workbook`, który sam brał 24,5 s i 281 MB *zanim* padł pierwszy wiersz — dlatego rozmiar po rozpakowaniu sprawdzamy z centralnego katalogu zipu, **przed** oddaniem pliku parserowi (`MAX_ROZPAKOWANE = 100 MB`; prawdziwy arkusz trenera ma 570 KB, czyli 180× zapasu). Iteracja przerywana na `MAX_ROWS+1` zamiast przycinania po fakcie. **(b) Upload bez limitu:** przed — plik 290 MB dawał **419 MB RSS serwera** (+291 MB, czyli dokładnie rozmiar pliku); po — **139 MB** (+11 MB). `storage.read_upload_capped` czyta najwyżej `LIMIT+1` bajtów, więc odpowiedź 422 i jej treść są **identyczne jak dotąd**. `food_catalog` dostał jawny limit, bo nie przechodzi przez `read_table` i ciche ucięcie zepsułoby ostatni wiersz. **SPROSTOWANIE:** pierwotnie wpisano tu 1057 MB dla (b) — ta liczba była **zawyżona**, bo pomiar szedł przez `TestClient` w tym samym procesie i obejmował bufor klienta. Serwer mierzony osobno brał 419 MB. Błąd był realny, skala mniejsza niż podana. | BYŁA ŚREDNIA | naprawione w `sheet_import.py`, `storage.py` i trzech routerach; 7 testów w `tests/test_import_pamiec.py` | — |
