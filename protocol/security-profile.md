# HOSP Security Profile 0.2

Processing order:

1. resolve identity,
2. verify active status,
3. validate key binding,
4. verify signature,
5. reject expired or replayed messages,
6. apply trust policy,
7. apply consent,
8. apply capability checks,
9. execute or deny,
10. issue a receipt.
