# ADR-0007: Human Model and Consent-Aware Personalization

## Status
Accepted — implemented in engine release 0.8 (`human_model`, `consent`,
`personalization`).

Human OS separates declarations, facts, observations, AI inferences and hypotheses. Personalization requires active, purpose-bound consent and exposes only the minimum domain projection. Users can contest records and revoke future access.
