# Przegląd bezpieczeństwa — 2026-08-17

**Commit:** `1f702c6` · **Wersja:** 0.10.0-alpha.1 · **Wykonawca:** sesja
Claude Code (agent AI) na zlecenie foundera.

**Deklaracja (DD-008):** to jest przegląd **wewnętrzny**, wykonany przez
agenta AI uczestniczącego w rozwoju tego kodu. **Nie jest niezależny** od
autorów ani od agentów AI użytych do budowy. Ta granica jest ryzykiem
zaakceptowanym (AR-001). Przegląd zewnętrzny pozostaje możliwy bez zmiany
protokołu.

Metoda: dla każdego obszaru — lektura ścieżki kodu, uruchomienie testów,
oraz co najmniej jedna **próba nadużycia** zakodowana jako test
w `tests/test_security_regression.py` (17 prób, wszystkie odparte).

## Wyniki per obszar (SECURITY_REVIEW_PROTOCOL.md §2)

| Poz. | Obszar | Wynik | Waga | Notatka |
|---|---|---|---|---|
| 2.1 | HOSP i podpisy | PASS | — | `canonical_json` deterministyczny (sort_keys, ensure_ascii=False, separatory bez spacji); podpis obejmuje całą kopertę; tamper payloadu i zły klucz unieważniają podpis; zniekształcony podpis → `False`, nie wyjątek. Dwie wersje kopert (SDK `HOSP/0.1` vs `HOSP/0.2`) to pole `protocol` w treści — rozróżnialne. HMAC = mechanizm referencyjny (AR-002). |
| 2.2 | Identity / Authority / Consent | PASS | — | Tożsamość nie-ACTIVE odrzucana w bramce; oś roli (`authority`) sprawdzana niezależnie od osi tożsamości; klucz musi być dowiązany do nadawcy. Zgoda: patrz `test_consent`. |
| 2.3 | Security Gateway | PASS | — | Kolejność zgodna z `protocol/security-profile.md`: sender → active → key binding → signature → replay/expiry → trust. Odmowa na wcześniejszym kroku zwraca `DENIED` przed późniejszymi. Brak domeny w payloadzie **nie** rozszerza do „*" wbrew polityce (test). |
| 2.4 | Replay Guard i wygasanie | PASS | — | Powtórzony `message_id`/`nonce` odrzucany; `expires_at` (epoch) < now → odrzucenie mimo ważnego podpisu. **Ograniczenie:** guard jest in-memory — po restarcie procesu okno powtórzeń się zeruje (AR-005). Zegar to zależność jawna (`now_epoch`). |
| 2.5 | Execution Loop | PASS | — | Odmowa na każdej bramie jako `IntentOutcome.REFUSED_*` (nie wyjątek); patrz `test_execution_loop` (13 przypadków). |
| 2.6 | Recovery + Emergency Root | PASS | — | AGENT/SERVICE/SYSTEM_PROCESS nie aktywuje ani nie deaktywuje — odmowa logowana; dual-key odrzuca tego samego inicjatora jako kustosza; **brak publicznego API mutującego politykę/audyt** (weryfikacja powierzchni `dir()`); Emergency Root niekonstruowalny bez polityki, zero wartości domyślnych. |
| 2.7 | Magazyny zdarzeń i integralność | PASS | — | `verify_chain()` wykrywa modyfikację i usunięcie rekordu (dwa testy manipulacji SQL bezpośrednio na pliku); odtworzenie stanu: `test_replay`; historia sprzed zmian słownika czytelna. |
| 2.8 | Granice agentów i delegacji | PASS | — | Delegowanie capability spoza manifestu delegatora odrzucane (`issubset`); wywołanie bez capability → `DENIED`; brama HUMAN_REQUIRED nieobchodzalna bez `human_approval_id`; scope prefiksowy nie daje się obejść podobną nazwą („healthcare" ≠ „health"). **Znana luka:** brak autoryzacji per-wywołanie z kontekstem łańcucha (AR-003). |
| 2.9 | Aktualność threat modelu | FINDING | NISKIE | `security/THREAT_MODEL.md` nie wymienia jeszcze modułów dodanych po jego napisaniu: `emergency_root`, `decision_scales`, kanoniczne typy `RECOVERY_*`. Sekcja agentowa jest aktualna. Zalecenie: dopisać te moduły przy najbliższej zmianie threat modelu — nie blokuje 0.9. |

## Ustalenia

- **U-001 (NISKIE):** threat model nie odzwierciedla najnowszych modułów
  (poz. 2.9). Plan: uzupełnić przy następnej edycji `THREAT_MODEL.md`.
  Nie blokuje zamknięcia 0.9.
  **ROZWIĄZANE 2026-08-17:** dodano sekcję „Sovereignty and recovery
  mechanisms" do `THREAT_MODEL.md` (aktywa, zagrożenia, mitygacje i jawne
  luki dla `emergency_root`, `decision_scales`, kanonicznych zdarzeń
  `RECOVERY_*`) oraz trzy odpowiadające pozycje w `SECURITY_BASELINE.md`.

Brak ustaleń o wadze KRYTYCZNEJ lub WYSOKIEJ.

## Ryzyka zaakceptowane

Pięć pozycji założonych w `ACCEPTED_RISKS.md` (AR-001…AR-005) — wszystkie
wymagają podpisu foundera. **Do czasu podpisu pozostają jako
zaproponowane, nie zaakceptowane.**

## Regresja zabezpieczeń

`tests/test_security_regression.py` — 17 prób nadużycia, 100% odparte na
commitcie `1f702c6`. Pełny zestaw: 197 testów zielonych, mypy 0, ruff
czysty.

## Werdykt

Warunki zamknięcia 0.9 z DD-008:
1. udokumentowany przegląd wg protokołu — **spełniony** (ten dokument),
2. usunięcie problemów KRYTYCZNYCH/WYSOKICH — **spełniony** (brak takich),
3. zapis ryzyk zaakceptowanych — **częściowo**: rejestr założony, czeka na
   podpis foundera (AR-001…AR-005),
4. test regresji zabezpieczeń — **spełniony** (zestaw zielony).

**Blokada zamknięcia 0.9:** jedyny pozostały warunek to **podpis foundera
pod rejestrem ryzyk** (`ACCEPTED_RISKS.md`). Ustalenie U-001 jest NISKIE
i nie blokuje. Punkt 0.9 pozostaje formalnie otwarty do złożenia tego
podpisu.

**Aktualizacja 2026-08-17 (później tego samego dnia):** founder podpisał
wszystkie pięć pozycji rejestru (AR-001…AR-005) — zgoda wyrażona wprost
w sesji roboczej. Tym samym **wszystkie cztery warunki DD-008 są
spełnione i punkt 0.9 roadmapy zostaje zamknięty** (odnotowane
w `ROADMAP.md`).
